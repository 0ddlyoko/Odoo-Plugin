package me.oddlyoko.odoo.listeners;

import com.intellij.openapi.vfs.newvfs.BulkFileListener;

public class FileListener implements BulkFileListener {
/*
    @Override
    public void before(@NotNull List<? extends VFileEvent> events) {
        System.out.println("FileListener.before");
        System.out.println("Size: " + events.size());
        System.out.println(events.stream().map(f -> f.getFile().getPath()).collect(Collectors.joining("\n")));
    }

    @Override
    public void after(@NotNull List<? extends VFileEvent> events) {
        System.out.println("FileListener.after");
        System.out.println("Size: " + events.size());
        System.out.println(events.stream().map(f -> f.getFile().getPath()).collect(Collectors.joining("\n")));
    }
 */
}
